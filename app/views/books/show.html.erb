<html>
    <body>
        <span class="container">
            <%= render "layouts/header" %>
            <%= render 'layouts/user', user: @user %>
            <%= render 'layouts/new', book: @book %>
      <div class="col-md-9" style="background-color:green">
         <h1>Book detail</h1>
     <table class="table table-striped">
      <thead>
        <tr>
          <strong>
            <th></th>
            <th>Title</th>
            <th>Opinion</th>
            </strong>
            <th colspan="3"></th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td>image</td>
            <td><%= @book.title %></td>
            <td><%= @book.opinion %></td>
                   <% if @book.user == current_user %>
            <td><%= link_to "Edit", book_path(@book), crass:"btn btn-primary btn-block active" %></td>
            <td><%= link_to "Destroy", book_path(@book), method: :delete %></td>
          <% end %>

          </tr>
          </tr>

      </tbody>
     </table>
   </div>
   </div>
 </div>
</body>
</html>