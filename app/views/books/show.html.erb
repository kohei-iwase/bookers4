        <span class="container">
          <span class="row">
            <%= render "layouts/header" %>
            <div class="col-xs-3" style="background-color:green">
            <%= render 'layouts/user', user: @book.user %>
            <%= render 'layouts/new'%>
          </div>
      <div class="col-xs-9" style="background-color:green">
         <h1>Book detail</h1>
     <table class="table table-striped">
      <thead>
        <tr>
          <strong>
            <th></th>
            <th>Title</th>
            <th>Opinion</th>
            </strong>
            <th colspan="3"></th>
        </tr>
      </thead>
      <tbody>
        <tr><td><%= link_to user_path(@book.user.id), target: "_blank" do %>
          <%= attachment_image_tag @book.user, :profile_image, :fill, 50, 50, format:'jpeg', class: "profile-thumb", fallback: "no_image.jpg" %>
            <br>
            <%= @book.user.name %><% end %></td>
            <td><%= @book.title %></td>
            <td><%= @book.body %></td>
                   <% if @book.user == current_user %>
            <td><%= link_to "Edit", edit_book_path(@book), crass:"btn btn-primary btn-block active" %></td>
            <td><%= link_to "Destroy", book_path(@book), method: :delete %></td>
          <% end %>
          </tr>
          </tr>
      </tbody>
     </table>
   </div>
   </div>
 </div>
</span>
</span>