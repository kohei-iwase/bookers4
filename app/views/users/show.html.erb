<html>
    <body>
        <span class="container">
            <%= render "layouts/header" %>
            <%= render 'layouts/user', user: @user %>
            <%= render 'layouts/new', book: @book %>
            <div class ="col-md-9" style="background-color: green;">
                <span crass="books-container">
                    <h1>Books</h1>
                    <table class="table table-striped">
                    <thead>
                        <tr>
                        <strong><th></th>
                            <th>Title</th>
                            <th>Opinion</th>
                        </strong>
                        </tr>
                    </thead>
                    <tbody>
                        <tr>
                    <% @books.each do |book| %>
                        <td>
                    <%= attachment_image_tag book.user, :profile_image, :fill, 100, 100, format: 'jpeg', class: "img-circle pull-left profile-img" %></td>
                        <td><%= link_to book.title, "/books/#{book.id}" %></td>
                        <td><%= book.opinion %></td>
                    </tr>
                     <% end %>
                    </tbody>
                    </table>
                </span>
            </div>
        </span>
    </body>
</html>