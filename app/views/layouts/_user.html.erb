                        <h2>User info</h2>
                        <div class="user-edit-image">
                        <%= attachment_image_tag user, :profile_image, :fill, 50, 50, format:'jpeg', class: "profile-thumb", fallback: "no_image.jpg" %>
                      </div>
                                <br>
                                    <table class="table table-striped">
	                                   <tr>
                                            <th>name</th>
                                            <th><%= user.name %></th>
                                        </tr>
	                                   <tr>
		                                  <th>introduction</th>
		                                  <th><%= user.introduction %></th>
	                                   </tr>
                                    </table>
	                               <% if user.id == current_user.id %>
                                        <div class ="col-xs-3 btn btn-default">
                                        <span class="glyphicon glyphicon-wrench" aria-hidden="true">
                                        <%= link_to "edit",edit_user_path(user),class: "pull-right" %></span>
                                        </div>
                                        <% end %>